# EDC Configuration Properties
# This file contains the required configuration for the Tractus-X EDC runtime

# Identity and Trust Configuration
edc.iam.issuer.id=did:web:example.com

# Data Plane Token Configuration
edc.transfer.proxy.token.signer.privatekey.alias=key-1
edc.transfer.proxy.token.verifier.publickey.alias=key-1

# HTTP Configuration
web.http.port=8080
web.http.path=/api
web.http.control.port=8084
web.http.control.path=/control
web.http.public.port=8081
web.http.public.path=/public
web.http.management.port=8181
web.http.management.path=/management
web.http.management.auth.key=secure-development-key-2024  # DEV-ONLY: This is a development credential, not for production

# Participant Configuration
edc.participant.id=test-participant
edc.runtime.id=test-runtime

# IATP/STS Configuration (required for Tractus-X)
edc.iam.sts.oauth.token.url=http://sts.example.com/token
edc.iam.sts.oauth.client.id=test-clientid
edc.iam.sts.oauth.client.secret.alias=test-clientid-alias
tx.edc.iam.iatp.bdrs.server.url=http://bdrs.example.com

# Data Plane Configuration
edc.dataplane.api.public.baseurl=http://localhost:8081/public/v2/data
tx.edc.dpf.consumer.proxy.port=8186
tx.edc.dpf.consumer.proxy.auth.apikey=password  # DEV-ONLY: This is a development credential, not for production

# Transfer Configuration
edc.transfer.send.retry.limit=1
edc.transfer.send.retry.base-delay.ms=100

# Catalog Configuration
edc.catalog.cache.execution.delay.seconds=2
edc.catalog.cache.execution.period.seconds=2

# Policy Configuration
edc.policy.validation.enabled=true

# Agent Identity Configuration
edc.agent.identity.key=BusinessPartnerNumber

# DSP Configuration
edc.dsp.callback.address=http://localhost:8080/api/protocol

# Vault Configuration (for secrets)
edc.vault.secrets=key1:secret1;key2:secret2
# Hashicorp Vault Configuration
edc.vault.hashicorp.url=http://localhost:8200
edc.vault.hashicorp.token=root  # DEV-ONLY: This is a development token, not for production

# Database Configuration (PostgreSQL)
# Default datasource configuration for PostgreSQL
edc.datasource.default.url=jdbc:postgresql://localhost:5433/edc
edc.datasource.default.user=user
edc.datasource.default.password=password  # DEV-ONLY: This is a development credential, not for production

# PostgreSQL Migration Schema
org.eclipse.tractusx.edc.postgresql.migration.schema=public
